# PPT 디자인 시스템 — "중국 SNS 완전정복" 템플릿

이 문서는 `build/build_deck.py`에서 쓰인 디자인 규칙을 정리한 것입니다. 새로운 주제의 PPT를 만들 때 이 문서 + `build_deck.py`를 Claude에게 함께 주면, 같은 톤앤매너로 덱을 다시 만들 수 있습니다.

## 만드는 방식

- PowerPoint 자체 파일(.pptx)을 Python의 `python-pptx` 라이브러리로 직접 코드 생성합니다.
- 완성 후 Windows PowerPoint COM 자동화로 실제 렌더링 이미지를 뽑아 눈으로 검수합니다(레이아웃 깨짐·겹침·잘림 확인).
- 디자인 툴(Figma 등) 없이 좌표 계산만으로 카드·배지·필(pill)·아이콘 배치를 직접 그립니다.

## 색상 팔레트

| 용도 | 이름 | 헥스 |
|---|---|---|
| 메인 레드 | RED | `C8102E` |
| 딥 레드(배경/카드) | DARK_RED | `3D0811` |
| 골드 포인트(킥커 라벨 등) | GOLD | `C9A227` |
| 배지/타이틀 강조 텍스트 | GLYPH_INK | `F7F7F5` |
| 본문 진한 회갈 | DARK | `221F1F` |
| 화이트 | WHITE | `FFFFFF` |
| 연한 카드 배경 | GRAY_CARD | `F3F3F3` |
| 흐린 보조 텍스트 | MUTED | `8A8383` |
| 어두운 배경 위 밝은 텍스트 | LIGHT_TXT | `EAE2DD` |

빨강 하나가 60~70%를 차지하고 골드는 포인트로만 쓰는 구조. 새 주제로 바꿀 때는 이 팔레트 전체를 주제 색으로 교체하면 됩니다.

## 폰트 규칙

- **큰 제목(슬라이드 헤드라인)**: `A2Z 7 Bold`
- **나머지 본문(부제목·카드명·불릿·통계 숫자 등)**: `A2Z 4 Regular`
- **본문에 섞인 한자**: 정규식으로 한자 유니코드 구간(`一-鿿`)을 자동 감지해 `Noto Sans SC Medium`으로만 별도 렌더링(나머지는 그대로 한/영 폰트 유지) — `add_run_mixed()` 함수 참고
- 새 폰트로 바꾸고 싶으면 Windows에 설치된 정확한 글꼴 패밀리명을 먼저 확인해야 함(예: "A2Z 7 Bold"는 파일명이 아니라 실제 글꼴 패밀리명)

## 레이아웃 공통 패턴

- 슬라이드 크기: 13.333 × 7.5인치(16:9)
- 콘텐츠 슬라이드 헤더 구조: 골드 킥커 라벨(예: "01 · OVERVIEW") → 큰 제목 → 가운데 정렬 문장형 부제목(핵심 구절만 레드 강조, `add_highlight_subtitle()`)
- 카드: `GRAY_CARD` 배경 + 둥근 모서리(radius 0.06~0.12), 내부에 아이콘/배지 + 텍스트
- 원형 배지: 빨간 원 + 흰색(`GLYPH_INK`) 한자 1글자 (`add_badge()`), 또는 실제 앱 로고를 흰 원판 위에 올려 타원(ellipse) 크롭 (`add_icon_circle()`)
- 필(pill) 라벨: 완전히 둥근 모서리의 작은 태그, 빨간 배경 + 흰 텍스트 (`add_pill()`)
- 표지: 흰 배경 위에 "폰 카드"가 중앙에 떠 있는 구조 — 어두운 베젤 테두리, 노치, 사이드 버튼, 안쪽은 레드 단색, 상태바 아이콘, 말풍선 장식, 하단 흰색 필 바(이름/부제)
- 마무리 슬라이드: 표지와 같은 레드 배경으로 통일감(북엔드 구조)

## 앱 아이콘을 원형으로 넣는 방법

1. 아이콘 원본이 정사각형이 아니면 Pillow로 투명 배경 정사각형 캔버스에 가운데 정렬해 패딩(`square_pad()`)
2. `add_shape`로 흰 원판(plate)을 먼저 그리고
3. 그 위에 패딩된 이미지를 원판보다 살짝 작게 삽입
4. 이미지 도형의 `spPr/prstGeom`을 `ellipse`로 바꿔 원형으로 크롭 (`crop_to_ellipse()`)

## 재사용 방법 (다음 프로젝트에서)

1. `build/build_deck.py`를 복사해 새 프로젝트 폴더에 둔다.
2. 색상 팔레트 상수들만 새 주제에 맞게 바꾼다.
3. 아이콘이 필요하면 `build/assets/icons/`에 새 이미지를 넣고 `ICON_SRC` 딕셔너리를 수정한다.
4. 슬라이드 내용(텍스트·카드 데이터)만 갈아 끼운다 — 레이아웃 함수(`add_heading`, `add_highlight_subtitle`, `add_badge`, `add_icon_circle`, `add_pill`, `add_bullets` 등)는 그대로 재사용 가능.
5. 빌드 후 PowerPoint COM으로 렌더링해 슬라이드별로 겹침/잘림 여부를 반드시 눈으로 확인한다.
